---

output-dir: out
threads: 16

git:
  user-name: cidr
  user-email: cidr@example.com


drivers:
  cisco7600:
    commands:
      - {cli: terminal length 0, display: no}
      - {cli: terminal width 0, display: no}
      - {cli: show module, quote: yes}
      - {cli: 'dir /all all-filesystems', quote: yes}
      - {cli: show vlan, quote: yes}
      - {cli: show configuration}
    filters:
      - 'Using \d+ out of \d+ bytes'

  cisco-catalyst:
    commands:
      - {cli: terminal length 0, display: no}
      - {cli: terminal width 0, display: no}
      - {cli: 'dir /all all-filesystems', quote: yes}
      - {cli: show vlan, quote: yes}
      - {cli: show configuration}
    filters:
      - 'Using \d+ out of \d+ bytes'

      
  foundry-fls:
    commands:
      - {cli: 'skip-page-display', display: no}
      - {cli: 'show chassis', quote: yes}
      - {cli: 'show module', quote: yes}
      - {cli: 'show flash', quote: yes}
      - {cli: 'show version', quote: yes}
      - {cli: 'show interface brief', quote: yes}
      - {cli: 'show conf'}
    filters:
      - 'skip-page-display'
      - 'Disable page display mode'
      - 'Startup-config data location is .*'
      - '(Startup|Current) configuration:'
      - '.* uptime is .* seconds?'
      - '.* temperature.* deg-C$'
      - '^ver .+'
      
  foundry-mlx:
    commands:
      - {cli: 'skip-page-display', display: no}
      - {cli: 'show chassis', quote: yes}
      - {cli: 'show module', quote: yes}
      - {cli: 'show flash', quote: yes}
      - {cli: 'show version', quote: yes}
      - {cli: 'show interface brief', quote: yes}
      - {cli: 'show conf'}
    filters:
      - 'skip-page-display'
      - 'Disable page display mode'
      - 'Startup-config data location is .*'
      - '(Startup|Current) configuration:'
      - '(Active|Standby) Management uptime is .* seconds?'
      - 'LP Slot \d+ uptime is .* seconds?'
      - '(Active|Standby|SNM|LP).*:.*\d+\.\d+C ?$'
      - '.* temperature.* deg-C$'
      - '^ver .+'

  juniper-m:
    comment: '#'
    commands:
      - { cli: 'set cli screen-length 0', display: no}
      - { cli: 'set cli screen-width 0', display: no}
      - { cli: 'set cli terminal ansi', display: no}
      - { cli: show chassis environment, quote: yes}
      - { cli: show chassis firmware, quote: yes}
      - { cli: show chassis fpc detail, quote: yes}
      - { cli: show chassis hardware detail, quote: yes}
      - { cli: show chassis alarms, quote: yes}
      - { cli: show system license, quote: yes}
      - { cli: show system boot-messages, quote: yes}
      - { cli: show system core-dumps, quote: yes}
      - { cli: show version detail, quote: yes}
      - { cli: show configuration}
    filters:
      - 'Uptime .* seconds?'
      - '\d+ degrees C / \d+ degrees F$'

# Device example
#
# devices:
#  
#   - name: c7600.example.com
#     username: cird
#     password: password-for-cidr
#     enable: enable-password
#     protocol: ssh
#     driver: cisco7600
#
#   # No need to enable
#   - name: sw1.example.com
#     username: cidr
#     password: password-for-cird
#     driver: foundry-fls
#     protocol: ssh
#     same-as:
#       - sw2.example.com
#       - sw3.example.com
#       - sw4.example.com
#
#   - name: jm8.example.com
#     username: cidr
#     password: password-for-cird
#     driver: juniper-m
#     protocol: ssh
#    
#   - name: swc.example.com
#     username: cidr
#     password: password-for-cird
#     driver: cisco-catalyst
#     protocol: ssh
